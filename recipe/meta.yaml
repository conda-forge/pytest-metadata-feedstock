{% set name = "pytest-metadata" %}
{% set version = "1.6.0" %}
{% set hash_type = "sha256" %}
{% set hash = "2f14521b06463d6e4db9a3a79eac95481fda9e3bac532b4b9b46a3572f6f0c3b" %}

package:
  name: {{ name }}
  version: {{ version }}

source:
  fn: {{ name }}-{{ version }}.tar.gz
  url: https://pypi.io/packages/source/p/{{ name }}/{{ name }}-{{ version }}.tar.gz
  {{ hash_type }}: {{ hash }}

build:
  noarch: python
  number: 0
  script: python setup.py install --single-version-externally-managed --record=record.txt

requirements:
  build:
    - python
    - setuptools
    - setuptools_scm
    - pytest >=2.9.0

  run:
    - python
    - setuptools
    - pytest >=2.9.0

test:
  imports:
    - pytest_metadata
    - pytest_metadata.ci

about:
  home: https://github.com/davehunt/pytest-metadata
  license: MPL 2.0
  license_family: OTHER
  license_file: LICENSE
  summary: 'pytest plugin for test session metadata'

extra:
  recipe-maintainers:
    - birdsarah
    - davehunt
    - nicoddemus
